.container-fluid
  = form_tag(orders_path, method: 'POST', onsubmit: 'return window.cartItemsCashier.canSubmit();') do
    .row
      .content-strip.primary
        .content-strip-inner
          h4.strip-title 購物書包
          p.strip-description
    = react_component 'CartItemsCashier', cartItems: @cart_items_data
